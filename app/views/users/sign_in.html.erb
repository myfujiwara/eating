<h3>サインイン</h3>
  <%= form_for @user, url: sign_in_path do |f| %>
    <div class="field">
      <%= f.label :"メールアドレス" %>
      <%= f.email_field :email %>
    </div>
    <div class="field">
      <%= f.label :"パスワード" %>
      <%= f.password_field :password %>
    </div>
    <div class="actions">
      <%= f.submit "ログイン" %>
    </div>
  <% end %>

  <div class="sign_in_link">
    <p>アカウントをお持ちですか？ <%= link_to new_user_path do %> 新規登録する <% end %></p>
  </div>
